# Dependencies
/node_modules

# Next.js build output
/.next
/out

# Environment secrets — never commit real credentials
.env
.env.local
.env*.local

# Local SQLite (legacy, pre-Supabase)
prisma/dev.db
prisma/dev.db-journal

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS/editor
.DS_Store
Thumbs.db

# Superseded static prototype (kept locally for reference only)
/legacy-static

# Raw uploaded source photos (served copy lives in /public/images/lookbook)
/images
.vercel
.env*
